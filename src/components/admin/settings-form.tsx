"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Loader2, Save } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input, Textarea } from "@/components/ui/input";

export interface AdminSettings {
  institution_name: string; questionnaire_name: string; logo_url: string; primary_color: string; secondary_color: string; intro_text: string; final_message: string; privacy_policy: string; consent_text: string; consent_version: string; emergency_message: string; emergency_contacts: Array<{ label: string; value: string }>; estimated_minutes: number; notification_emails: string[]; session_timeout_minutes: number; invite_message_template: string;
}
export function SettingsForm({ initial }: { initial: AdminSettings }) { const router = useRouter(); const [values, setValues] = useState(initial); const [loading, setLoading] = useState(false); const set = (key: keyof AdminSettings, value: unknown) => setValues((current) => ({ ...current, [key]: value })); async function submit(event: React.FormEvent) { event.preventDefault(); setLoading(true); try { const response = await fetch("/api/admin/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(values) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Não foi possível salvar."); toast.success("Configurações atualizadas."); router.refresh(); } catch (e) { toast.error(e instanceof Error ? e.message : "Erro"); } finally { setLoading(false); } }
  return <form onSubmit={submit} className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Identidade visual</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Nome da instituição"><Input value={values.institution_name} onChange={(e) => set("institution_name", e.target.value)} required /></Field><Field label="Nome do questionário"><Input value={values.questionnaire_name} onChange={(e) => set("questionnaire_name", e.target.value)} required /></Field><Field label="URL da logo"><Input type="url" value={values.logo_url} onChange={(e) => set("logo_url", e.target.value)} placeholder="https://..." /></Field><Field label="Cor principal"><div className="flex gap-2"><Input type="color" value={values.primary_color} onChange={(e) => set("primary_color", e.target.value)} className="w-20 p-1" /><Input value={values.primary_color} onChange={(e) => set("primary_color", e.target.value)} /></div></Field><Field label="Cor secundária"><div className="flex gap-2"><Input type="color" value={values.secondary_color} onChange={(e) => set("secondary_color", e.target.value)} className="w-20 p-1" /><Input value={values.secondary_color} onChange={(e) => set("secondary_color", e.target.value)} /></div></Field></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Textos públicos</h2><div className="mt-5 space-y-4"><Field label="Texto introdutório"><Textarea value={values.intro_text} onChange={(e) => set("intro_text", e.target.value)} /></Field><Field label="Mensagem final"><Textarea value={values.final_message} onChange={(e) => set("final_message", e.target.value)} /></Field><Field label="Política de privacidade"><Textarea value={values.privacy_policy} onChange={(e) => set("privacy_policy", e.target.value)} className="min-h-48" /></Field><Field label="Termo de consentimento"><Textarea value={values.consent_text} onChange={(e) => set("consent_text", e.target.value)} /></Field><div className="grid gap-4 md:grid-cols-2"><Field label="Versão do consentimento"><Input value={values.consent_version} onChange={(e) => set("consent_version", e.target.value)} /></Field><Field label="Tempo estimado (minutos)"><Input type="number" min={5} max={60} value={values.estimated_minutes} onChange={(e) => set("estimated_minutes", Number(e.target.value))} /></Field></div></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Segurança emocional e notificações</h2><div className="mt-5 space-y-4"><Field label="Mensagem de emergência"><Textarea value={values.emergency_message} onChange={(e) => set("emergency_message", e.target.value)} /></Field><Field label="Contatos de emergência (um por linha no formato Nome: contato)"><Textarea value={values.emergency_contacts.map((item) => `${item.label}: ${item.value}`).join("\n")} onChange={(e) => set("emergency_contacts", e.target.value.split("\n").filter(Boolean).map((line) => { const [label, ...rest] = line.split(":"); return { label: label.trim(), value: rest.join(":").trim() }; }))} /></Field><Field label="E-mails autorizados para alertas (um por linha)"><Textarea value={values.notification_emails.join("\n")} onChange={(e) => set("notification_emails", e.target.value.split("\n").map((v) => v.trim()).filter(Boolean))} /></Field><Field label="Encerrar sessão após minutos de inatividade"><Input type="number" min={5} max={240} value={values.session_timeout_minutes} onChange={(e) => set("session_timeout_minutes", Number(e.target.value))} /></Field></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Mensagem de convite (WhatsApp)</h2><p className="mt-1 text-sm text-slate-500">Use <code className="rounded bg-slate-100 px-1">{"{NOME}"}</code> para o nome da paciente e <code className="rounded bg-slate-100 px-1">{"{LINK}"}</code> para o link do questionário. Essa mensagem é usada na tela de Convites para gerar o texto pronto para copiar e enviar.</p><div className="mt-4"><Textarea value={values.invite_message_template} onChange={(e) => set("invite_message_template", e.target.value)} className="min-h-40" /></div></section><Button size="lg" disabled={loading}>{loading ? <Loader2 className="animate-spin" /> : <Save />}{loading ? "Salvando..." : "Salvar configurações"}</Button></form>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block"><span className="mb-1.5 block text-sm font-semibold">{label}</span>{children}</label>; }
