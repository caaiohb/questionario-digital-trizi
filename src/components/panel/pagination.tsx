import Link from "next/link";
import { ChevronLeft, ChevronRight } from "lucide-react";
export function Pagination({ page, pages, searchParams }: { page: number; pages: number; searchParams: Record<string, string | undefined> }) { if (pages <= 1) return null; const href = (next: number) => { const params = new URLSearchParams(); for (const [key, value] of Object.entries(searchParams)) if (value && key !== "page") params.set(key, value); params.set("page", String(next)); return `?${params.toString()}`; }; return <nav aria-label="Paginação" className="mt-5 flex items-center justify-between"><Link aria-disabled={page <= 1} className={`inline-flex items-center gap-2 rounded-xl border px-4 py-2 text-sm font-semibold ${page <= 1 ? "pointer-events-none opacity-40" : "hover:bg-white"}`} href={href(page - 1)}><ChevronLeft size={17} />Anterior</Link><span className="text-sm text-slate-600">Página <strong>{page}</strong> de <strong>{pages}</strong></span><Link aria-disabled={page >= pages} className={`inline-flex items-center gap-2 rounded-xl border px-4 py-2 text-sm font-semibold ${page >= pages ? "pointer-events-none opacity-40" : "hover:bg-white"}`} href={href(page + 1)}>Próxima<ChevronRight size={17} /></Link></nav>; }
