import type { LucideIcon } from "lucide-react";
import { Card } from "@/components/ui/card";
export function StatCard({ label, value, icon: Icon, tone = "default" }: { label: string; value: number; icon: LucideIcon; tone?: "default" | "warning" | "danger" | "success" }) { const styles = { default: "bg-[var(--trizi-primary)]/8 text-[var(--trizi-primary)]", warning: "bg-amber-100 text-amber-800", danger: "bg-red-100 text-red-800", success: "bg-emerald-100 text-emerald-800" }; return <Card className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-slate-900">{value.toLocaleString("pt-BR")}</p></div><div className={`grid h-11 w-11 place-items-center rounded-xl ${styles[tone]}`}><Icon size={21} /></div></div></Card>; }
